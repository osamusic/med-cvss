# Production environment variables for Vite build
# Prevent warnings from being treated as errors in CI/CD
CI=false
# Disable source maps in production for security and smaller bundle size
GENERATE_SOURCEMAP=false